arr = list(map(int,input("enter array: ").split()))
d = int(input("enter length to be skipped: "))
'''for i in range(d):
    arr.append(arr.pop(0))
print(arr)'''
'''d = d % len(arr)  
arr = arr[d:] + arr[:d]
print(*arr)'''
